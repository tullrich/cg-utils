file(GLOB FILE_CPPS "*.cpp" "import/*.cpp")

IF(ASSIMP_ENABLED)
  file(GLOB ASSIMP_FILE_CPP "import/assimp/*.cpp")
  SET(FILE_CPPS ${FILE_CPPS} ${ASSIMP_FILE_CPP})
ENDIF(ASSIMP_ENABLED)

IF(RENDERSYSTEM_ENABLED)
	file(GLOB RENDERSYS_FILE_CPP "rendersystem/*.cpp")
	SET(FILE_CPPS ${FILE_CPPS} ${RENDERSYS_FILE_CPP})

	# Rocket
	file(GLOB ROCKET_FILE_CPP "gui/*.cpp")
	SET(FILE_CPPS ${FILE_CPPS} ${ROCKET_FILE_CPP})

	IF(OPENGL_ENABLED)
	  file(GLOB OPENGL_FILE_CPP "rendersystem/opengl/*.cpp")
	  SET(FILE_CPPS ${FILE_CPPS} ${OPENGL_FILE_CPP})
	ENDIF(OPENGL_ENABLED)
ENDIF(RENDERSYSTEM_ENABLED)

SET(CGUTILS_SRCFILES ${CGUTILS_SRCFILES} ${FILE_CPPS} PARENT_SCOPE)
